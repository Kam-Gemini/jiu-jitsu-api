<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Divisions</title>
</head>
<body>
    <h1>All Divisions</h1>
    <ul>
        <% allDivisions.forEach((division) => { %>
         <li>
            <a href="/divisions/<%= division._id %>">
            <%= division.belt %></a>
         </li>
        <% }) %>
    </ul>
    <% if (isLoggedIn) { %>
        <button onclick="window.location.href='/divisions/new'">Add New Division</button>
    <% } else { %>
        <p>Please log in to add a new division.</p>
    <% } %>
</body>
</html>